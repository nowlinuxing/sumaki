# frozen_string_literal: true

RSpec.describe Sumaki do
  it 'has a version number' do
    expect(Sumaki::VERSION).not_to be_nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
